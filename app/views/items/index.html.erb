<div class="text-center mb-4">
  <h1>投稿された記事一覧</h1>
</div>
<div class="row">
  <div class="col-sm-8 offset-sm-2">
    <% if @items.any? %>
      <%= render 'items', items: @items %>
    <% end %>
    <%= link_to '新しい記事を投稿する', new_item_path, class: 'btn btn-info' %>
  </div>
</div>